#include "holberton.h"
/**
 * add - adds two integers
 * @n: is a num
 * @m: is a num
 * Return: the sum of two integers
 */
int add(int n, int m)
{
	int result = 0;

	result = n + m;
	return (result);
}
