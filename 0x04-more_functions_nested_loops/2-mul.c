#include "holberton.h"
/**
 * mul - multiplies two integers
 * @a: is the first integer
 * @b: is the secon number
 * Return: the multiplication result
 */
int mul(int a, int b)
{
	int m = 0;

	m = a * b;
	return (m);
}
