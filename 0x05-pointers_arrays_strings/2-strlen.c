#include "holberton.h"
/**
 * _strlen - Counts the numbers of characteres that a string have
 * @s: Number of characteres in a string
 * Return: 0 Always
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{

	}
	return (i);
}
