#include <stdio.h>
#include "holberton.h"
/**
 * main - Print the name's file
 * @argc: Number os arguments in command line
 * @argv: Argument vector
 * Return: 0 always
 */
int main(int argc, char *argv[])
{
	int i = 0;
	
	printf("%s\n", argv[i]);
	(void)argc;

	return (0);
}
